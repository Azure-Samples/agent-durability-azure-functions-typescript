{
  "name": "multi-agent-wrapper-sample",
  "version": "1.0.0",
  "description": "Multi-agent Azure Functions app using simplified Tool and Durable wrappers",
  "main": "dist/function-app.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && func start",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "durable-functions": "^3.0.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}